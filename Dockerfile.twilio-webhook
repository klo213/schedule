FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["sh", "-c", "python -u twilio_coach_request_webhook.py --config config/config.yaml --host 0.0.0.0 --port ${PORT:-8080} --path /twilio/coach-request"]
